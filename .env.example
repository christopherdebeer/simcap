# GitHub Branching Storage
# -----------------------
# Required for uploading session data and visualizations to GitHub branches

# GitHub Personal Access Token with repo write access
# Required for: Python uploads (ml/github_upload.py), API proxy, manifest generation
# Scopes needed: repo (full control of private repositories)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Secret for browser-based uploads via API proxy
# Used to authenticate client requests to /api/github-upload
# Generate with: openssl rand -hex 32
SIMCAP_UPLOAD_SECRET=your-secret-here

# Optional Configuration
# ----------------------

# API base URL for fetch scripts (defaults to production)
# SIMCAP_API_URL=https://simcap.vercel.app

# GitHub repository (defaults to christopherdebeer/simcap)
# GITHUB_REPO=christopherdebeer/simcap
